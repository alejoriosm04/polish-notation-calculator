#pragma once
#include <iostream>
#include <string>
#include <vector>

using namespace std;

class Expression {
    private:
        
    public:
        Expression();
        void splitString(string &str, char delimeter, vector <string> &expression);

};
